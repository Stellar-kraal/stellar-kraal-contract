[package]
name = "carbon-credit-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[workspace]
# Standalone cargo-fuzz workspace: keeps the wasm-oriented release profile
# (lto = true, codegen-units = 1, strip = "symbols") pinned at the root
# workspace out of this crate's build — that combination corrupts
# ASan sancov symbol generation ("undefined symbol: __sancov_gen_*") when
# inherited into a sanitizer build.

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
arbitrary = { version = "1.3", features = ["derive"] }
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
carbon_registry = { path = "../../carbon_registry" }

[dependencies.carbon_credit]
path = ".."

[patch.crates-io]

[[bin]]
name = "mint"
path = "fuzz_targets/mint.rs"
test = false
doc = false

[[bin]]
name = "burn"
path = "fuzz_targets/burn.rs"
test = false
doc = false

[[bin]]
name = "retire"
path = "fuzz_targets/retire.rs"
test = false
doc = false

[[bin]]
name = "transfer"
path = "fuzz_targets/transfer.rs"
test = false
doc = false

[[bin]]
name = "batch"
path = "fuzz_targets/batch.rs"
test = false
doc = false
